# <Capability Area> — Coverage Findings

> **Section template — DO NOT edit this file to record findings.**
> Copy this shape into the matching `NN-<area>.md` section file and fill it in.
> Leading-underscore files are excluded from the rendered docs index, so this
> template never ships as a "finding."

| Field            | Value                                                              |
| ---------------- | ----------------------------------------------------------------- |
| Capability area  | `<area slug>`                                                     |
| Field-guide ref  | `field-guide-NN-<chapter>.html` (chapter N)                       |
| Bead-chain owner | `bead_chain-XXX` (the audit bead that fills this section)          |
| Primary modules  | `module.py`, `other.py`                                           |
| Status           | `not-started` \| `in-progress` \| `done`                          |

---

## 1. AVAILABLE — what the field guide documents

What does bd 1.0.4 actually expose for this capability? Summarize the feature
surface **with a chapter reference** so a reader can trace the claim.

- Cite the chapter: `field-guide-NN-<chapter>.html § "<section heading>"`.
- One bullet per discrete feature/sub-command/field/flag.
- Stick to what the guide *says exists* — not what bead-chain does with it.

## 2. LEVERAGED — what bead-chain actually uses

What does bead-chain consume of the above? **Every claim needs a `file:line`
citation** into the bead-chain source so the synthesis is verifiable.

- `prompt.py:258` — `format_bead_as_goal()` injects the bead's title/description…
- `lifecycle.py:379` — `pick_next_bead()` honors `blocks` edges via…
- If a feature is consumed indirectly (via a `bd` shell-out), cite the call site.
- If a feature is **not** leveraged at all, say so explicitly here (and open a
  GAP below) — silence is not a finding.

## 3. GAPS — what's missing, and how much it matters

One row per gap. Severity is **P0–P4** (P0 = data-loss / correctness hazard in
the driver loop; P4 = cosmetic / nice-to-have). Each gap gets a one-line
recommended follow-up that a future bead could be filed from verbatim.

| #   | Gap (one line)                                  | Severity | Recommended follow-up (one line)               |
| --- | ----------------------------------------------- | -------- | ---------------------------------------------- |
| 1   | `<what bead-chain ignores / mishandles>`        | P2       | `<file a bead to … >`                          |
| 2   |                                                 |          |                                                |

### Severity rubric

| Sev | Meaning                                                                       |
| --- | ---------------------------------------------------------------------------- |
| P0  | Correctness/data-loss hazard in the drain loop (e.g. closes wrong bead).      |
| P1  | Feature silently dropped where it changes which bead runs or how it's framed. |
| P2  | Feature unused where leveraging it would materially improve goal quality.     |
| P3  | Minor gap; workaround exists or impact is narrow.                            |
| P4  | Cosmetic / future-proofing only.                                            |

---

## Notes / open questions

Anything that needs a human decision or a cross-section dependency. Keep it
short — real follow-ups belong in the GAPS table as filed-bead candidates.
